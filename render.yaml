services:
  - type: web
    name: hive-checkout
    env: node
    plan: starter
    buildCommand: npm install --production
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: HMAC_SECRET
        generateValue: true
